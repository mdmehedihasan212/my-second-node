/**
 * when fetch data from localhost:5000
 * then go express js and middleware cors
 * npm i cors
 * declare cors and function
 * when create form for data post 
 * search fetch post data look developer.mozila docs
 * app.post server side function and console log req
 * if error then go middleware body parser just app.use(express.json());
 */